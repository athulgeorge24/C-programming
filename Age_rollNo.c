#include <stdio.h>
int main()
{
	int rollNo,age;
	printf("Enter the roll number and the age:");
	scanf("%d,%d",&rollNo,&age);
	printf("The roll number is %d and the age is %d",rollNo,age);
	return 0;
}
